version: "3.9"
services:
  backend:
    build:
      context: ./backend
    image: voxassist-backend:dev
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - JWT_SECRET=dev-secret
