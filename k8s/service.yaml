apiVersion: v1
kind: Service
metadata:
  name: voxassist-backend
  labels:
    app: voxassist-backend
spec:
  selector:
    app: voxassist-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
